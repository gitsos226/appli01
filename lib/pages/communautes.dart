import 'package:flutter/material.dart';

class CommunautesPage extends StatelessWidget {
  const CommunautesPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('page des communautes'));
  }
}
