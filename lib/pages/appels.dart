import 'package:flutter/material.dart';

class AppelsPage extends StatelessWidget {
  const AppelsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('page des appels'));
  }
}
