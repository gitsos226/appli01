import 'package:flutter/material.dart';

class ActusPage extends StatelessWidget {
  const ActusPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('page des actualités'));
  }
}
